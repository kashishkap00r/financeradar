name: Update FinanceRadar

"on":
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

concurrency:
  group: financeradar-hourly-main
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    timeout-minutes: 25
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate secrets
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
          TWITTER_ACCOUNTS_JSON: ${{ secrets.TWITTER_ACCOUNTS_JSON }}
        run: |
          [ -n "$TELEGRAM_API_ID" ] || (echo "::error::TELEGRAM_API_ID not set" && exit 1)
          [ -n "$TELEGRAM_API_HASH" ] || (echo "::error::TELEGRAM_API_HASH not set" && exit 1)
          [ -n "$TELEGRAM_SESSION" ] || (echo "::error::TELEGRAM_SESSION not set" && exit 1)
          [ -n "$TWITTER_ACCOUNTS_JSON" ] || (echo "::error::TWITTER_ACCOUNTS_JSON not set" && exit 1)

      - name: Fetch Telegram reports
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
        run: python3 telegram_fetcher.py

      - name: Run aggregator
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TWITTER_ACCOUNTS_JSON: ${{ secrets.TWITTER_ACCOUNTS_JSON }}
        run: python3 aggregator.py

      - name: Commit and push if changed
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add index.html static/articles.json static/telegram_reports.json static/youtube_cache.json static/reports_cache.json static/published_snapshot.json static/papers_cache.json static/twitter_url_cache.json
            for optional_file in static/twitter_clean_cache.json static/twitter_user_cache.json; do
              if [ -f "$optional_file" ]; then
                git add "$optional_file"
              fi
            done
            git commit -m "Update news feeds"
            git pull --no-rebase origin main
            git push
          fi
